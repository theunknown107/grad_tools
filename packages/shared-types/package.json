{
  "name": "@gradtools/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas that ARE the API contract. Imported by both the API and the web app so a contract change breaks both sides at compile time.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "zod": "^4.1.13"
  }
}
