# GradTools web — public configuration only.
#
# VITE_ variables are COMPILED INTO THE BROWSER BUNDLE. A secret must never
# appear here. The database URL and every server secret live in
# services/api/.env, and the browser never sees them (M5a §13).

# Base URL of the GradTools reference API.
VITE_API_URL=http://localhost:3001
