# services/

Backend services.

**Empty by design.** `services/api` (Express + scheduler + workers) is backlog item
**M2.3**, not yet approved for implementation. See `docs/33_M2_IMPLEMENTATION_BACKLOG.md`.
